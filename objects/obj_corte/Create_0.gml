hspeed = 10; // Velocidade do tiro
dano = 80;   // Dano ajustado para 60 PD

// Salva a posição X de nascimento do tiro
x_inicio = x; 

// Tamanho aproximado de 1 bloco/quadrado do grid em pixels (ajuste o 64 se o seu bloco tiver outro tamanho, ex: 80, 32)
tamanho_bloco = 100; 
alcance_maximo = tamanho_bloco * 2; // Alcance exato de 2 blocos